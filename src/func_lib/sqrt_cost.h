#include "../global_config.h"
#include "../tools/Matrix.h"
namespace Sqrt_Cost_Func {
    long double sqrt_cost(const Vector &a, const Vector &y);
    Vector d_sqrt_cost(const Vector &a, const Vector &y);
};